<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.ksh3490.myblog.board.replyMapper">
	<select id="getReplyList" resultType="com..ksh3490.myblog.board.model.ReplyVO">
		SELECT
			RID,
			BID,
			CONTENT,
			REG_ID,
			REG_DT,
			EDIT_DT
		FROM
			TBL_REPLY
		WHERE
			BID = #{bid}
		ORDER BY REG_DT desc
	</select>

</mapper>
